apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: devsecops-wordpress
spec:
  selector:
    app.kubernetes.io/name: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
